VITE_API_BASE_URL=http://localhost:8000/api/v1
# Optional bot protection on login — leave blank to disable. Get a free site key at
# https://dash.cloudflare.com/?to=/:account/turnstile (pair with TURNSTILE_SECRET_KEY
# in backend/.env — see backend/.env.example).
VITE_TURNSTILE_SITE_KEY=
